# Instalamos las librerías para cargar el curso
install.packages(c('swirl', 'swirlify'))

# Cargamos las librerías en la sesión de trabajo
library(swirl)
library(swirlify)

# Instalamos el curso
install_course()

#Cargamos el curso
swirl()

# Debes selecionar para seguir 

# Presione 0 o "escape" para salir del curso una vez enviada la respuesta.
